# Kodilla Library

## How to run locally

1. Clone repository:
   git clone https://github.com/bartlomiejlorenowicz/Kodilla-Library.git

2. Start database:
   docker compose up -d

3. Run application:
   ./gradlew bootRun

Application runs on:
http://localhost:8080
